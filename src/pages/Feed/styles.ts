import styled from 'styled-components'

export const FeedContainer = styled.main`
  width: 54rem;
  min-height: 80rem;

  margin: -90px auto 0;

  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4.75rem;
`
